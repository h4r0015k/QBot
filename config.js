function config() {

    this.token = ""; // Add telegram bot token here.
    this.bot = 0; // Add telegram main group id here.	
    this.interval = 5; // Add interval time (minutes) here.   
    this.response = "correct answer!"; // Add reponse for correct answer here.

}

module.exports = config;
